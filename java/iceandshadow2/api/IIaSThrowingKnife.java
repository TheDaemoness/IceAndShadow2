package iceandshadow2.api;

import net.minecraft.item.ItemStack;
import net.minecraft.world.World;

/**
 * A list of functions a throwing knife entity is guaranteed to have.
 * This is NOT existent on the actual throwing knife item itself.
 * Can be used to check if an entity is a throwing knife by instanceof.
 * This should NOT be applied on any other entities.
 */
public interface IIaSThrowingKnife {
}
